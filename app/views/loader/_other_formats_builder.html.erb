<%= javascript_include_tag 'hide_other_formats_links.js', :plugin => 'redmine_loader' %>
<% other_formats_links do |f| %>
  <%= f.link_to_with_query_parameters 'Atom', :key => User.current.rss_key %>
  <%= f.link_to_with_query_parameters 'CSV', {}, :onclick => "showModal('csv-export-options', '350px'); return false;" %>
  <%= f.link_to_with_query_parameters 'PDF' %>
  <%= f.link_to_with_query_parameters 'XML', query_id: @query.id if params[:project_id] && User.current.allowed_to?(:export_issues_to_xml, @project) %>
<% end %>